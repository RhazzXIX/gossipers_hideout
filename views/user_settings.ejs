<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <%- include('header') -%>
    <h1><%= title %></h1>
    <form action="" method="post">
      <% if (currentUser.isMember === false) { %>
      <p>Want to be a member?</p>
      <label for="memberCode">
        Enter secret code:
        <input type="text" name="memberCode" id="memberCode" required="true" />
      </label>
      <%}%> 
      <% if (currentUser.isAdmin === false) {%>
      <label for="applyForAdmin">
        <% if (currentUser.isMember) {%>
        <input type="checkbox" name="applyForAdmin" id="applyForAdmin" value="yes" required/>
        <%} else {%>
        <input type="checkbox" name="applyForAdmin" id="applyForAdmin" value="yes"/>
        <%}%> 
        Apply for admin 
      </label>
      <%}%>
      <button type="submit">Submit</button>
    </form>
    <a href="/settings/log-out" class="button">Log out</a>
  </body>
</html>
